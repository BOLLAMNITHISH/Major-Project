# Required: path to PyTorch checkpoint from model/train_vit_pytorch.py
VIT_CHECKPOINT_PATH=../model/checkpoints_vit_torch/best_model.pth

# Optional: overrides auto-discovery (same folder as checkpoint or ./data/evaluation_report.json)
# EVALUATION_REPORT_PATH=../model/checkpoints_vit_torch/evaluation_report.json

# Optional
# DATABASE_URL=sqlite+aiosqlite:///./data/predictions.db
